<h1>Welcome#index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>

<%= form_tag do %>

<% end %> 
 
  <form action="/welcome/index" class="new_user" id="new_user" method="get">
    <label for="user_name">Name</label>
    <input id="user_name" name="user[name]" type="text" />
    <input id="user_name" name="user[name2]" type="text" />
    <input type="submit" />
  </form>



<head> 
  <meta http-equiv="content-type" content="text/html; charset=UTF-8"> 
  <title>Google Maps Multiple Markers</title> 
  <script src="http://maps.google.com/maps/api/js?sensor=false"></script>
</head> 
<body>
  <div id="map" style="width: 500px; height: 400px;"></div>
  
  <p id="demo3"></p>
<script>
  var my_stuff = <%=@controller_message %>;
  document.getElementById("demo3").innerHTML = my_stuff;
</script>


<h2><%= @client.download_file(4547, 890157) %> </h2>
<% @coords = @client.download_file(4547, 890157) %>
<%= @point1 = @coords.split(',') %>

<%= @looped_message %>


<% @erb_array = [10,20,30,40] %> 

<%= @erb_array.each { |x| puts x } %>




  <script>
    // Define your locations: HTML content for the info window, latitude, longitude
    var locations = [
      ['<h4>Bondi Beach</h4>', <%= @point1[0] %>, <%= @point1[0] %>],
      ['<h4>Coogee Beach</h4>', <%= @point1[1] %>, <%= @point1[1] %>],
      ['<h4>Cronulla Beach</h4>', <%= @point1[2] %>, <%= @point1[2] %>],
      ['<h4>Manly Beach</h4>', <%= @point1[3] %>, <%= @point1[3] %>],
      ['<h4>Maroubra Beach</h4>', <%= @point1[4] %>, <%= @point1[4] %>]
    ];
    
   
   var clicklats = [];
   var clicklongs = []; 

    // Setup the different icons and shadows
    var iconURLPrefix = 'http://maps.google.com/mapfiles/ms/icons/';
    
    var icons = [
      iconURLPrefix + 'red-dot.png',
      iconURLPrefix + 'green-dot.png',
      iconURLPrefix + 'blue-dot.png',
      iconURLPrefix + 'orange-dot.png',
      iconURLPrefix + 'purple-dot.png',
      iconURLPrefix + 'pink-dot.png',      
      iconURLPrefix + 'yellow-dot.png'
    ]
    
    var array1 = []; 
   var js_coords = <%= @point1[0] %>;
   var erb_var = <%= @erb_array.each { |x| puts x } %>;


 // alert(js_coords)
    //js_coords.split(","); 
    //var cent_lat = array1[0];
    //var cent_lng = array1[1]; 

// to do: 
// google maps -> add points to form from clicking 
// write points into a local file 
// github! 
// style the tutorial    // github 


    var iconsLength = icons.length;

    var map = new google.maps.Map(document.getElementById('map'), {
      zoom: 10,
      center: new google.maps.LatLng(20,20),
      mapTypeId: google.maps.MapTypeId.ROADMAP,
      mapTypeControl: false,
      streetViewControl: false,
      panControl: false,
      zoomControlOptions: {
         position: google.maps.ControlPosition.LEFT_BOTTOM
      }
    });

 google.maps.event.addListener(map, 'click', function(event) {
    placeMarker(event.latLng);
   // alert("annoying message!");
  });

function placeMarker(location) {
  var marker = new google.maps.Marker({
    position: location,
    map: map,
  });
  var infowindow = new google.maps.InfoWindow({
    content: 'Latitude: ' + location.lat() + '<br>Longitude: ' + location.lng()
  });
  infowindow.open(map,marker);
  clicklats.push(location.lat());
  clicklongs.push(location.lng()); 
}


    var infowindow = new google.maps.InfoWindow({
      maxWidth: 160
    });

    var markers = new Array();
    
    var iconCounter = 0;
    
var array_2 = [0,0]; 


  for(i=0; i<5; i++){
   //alert(erb_var[i]);
   var stuff3 = erb_var[i]; 
   array_2.push(stuff3);
//   alert(array_2[i]);
 }
alert(array_2);


    // Add the markers and infowindows to the map
    for (var i = 0; i < locations.length + 1; i++) {  
      var marker = new google.maps.Marker({
        position: new google.maps.LatLng(array_2[i], array_2[i]),
        map: map,
        icon: icons[iconCounter]
      });

      markers.push(marker);

      google.maps.event.addListener(marker, 'click', (function(marker, i) {
        return function() {
          infowindow.setContent(array_2[i]);
          infowindow.open(map, marker);
        }
      })(marker, i));
      
      iconCounter++;
      // We only have a limited number of possible icon colors, so we may have to restart the counter
      if(iconCounter >= iconsLength) {
        iconCounter = 0;
      }
    }

    function autoCenter() {
      //  Create a new viewpoint bound
      var bounds = new google.maps.LatLngBounds();
      //  Go through each...
      for (var i = 0; i < markers.length; i++) {  
        bounds.extend(markers[i].position);
      }
      //  Fit these bounds to the map
      map.fitBounds(bounds);
    }
    autoCenter();
  </script> 
</body>


<% @workspaces['items'].each do |workspace| %>
  <% workspace = workspace['workspace'] %>
  <h2><%= workspace['name'] %></h2>
    

<h2><%= @client.download_file(4547, 890167) %> </h2>
<% @coords = @client.download_file(4547, 890167) %>


<% end %>

 

<script>
  var msg = <% @controller_message %>;
  alert(msg);

</script> 


<%= console %>